spring:
  application:
    name: payment

  config:
    import: optional:configserver:http://localhost:8888   # DÒNG NÀY BẮT BUỘC

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
#logging:
#  level:
#    org.springframework.web: DEBUG
#    org.springframework.web.filter: DEBUG
#    com.ecommerce.payment: TRACE  # Gói package của PaymentController